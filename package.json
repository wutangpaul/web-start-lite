{
  "devDependencies": {
    "webpack": "^1.13.2",
    "webpack-dev-server": "^1.15.1",
    "node-sass": "^3.9.3",
    "browser-sync": "^2.15.0",
    "concurrently": "^2.2.0",
    "npm-watch": "^0.1.6"
  },
  "private": true,
  "scripts": {
    "test": "whatever",
    "clean": "rm -rf dist/*",
    "copy:html": "cp src/index.html dist/",
    "watch:html": "watch \"npm run copy:html\" src/",
    "watch:css": "node-sass -w --include-path scss src/scss/main.scss dist/css/main.css",
    "build:js": "webpack",
    "build:css": "node-sass --include-path scss src/scss/main.scss dist/css/main.css",
    "build": "npm run clean && npm run copy:html && npm run build:css && webpack",
    "serve": "npm run build && concurrently \"npm run watch:html\" \"npm run watch:css\" \"webpack-dev-server --content-base dist/\" \"browser-sync start --proxy localhost:8080 --files dist\""
  }
}
